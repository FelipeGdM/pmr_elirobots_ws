## Intro

This package manages the lifecycle of a EC elite robot

### On startup

In the startup, the node must:

- Clear alarms
- Sync encoders
- Enable servos

## Architecture

## Dependencies

```bash
sudo apt install libargtable2-dev
```

